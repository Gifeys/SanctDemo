<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- The Scan tab opens the camera for AI recognition and QR codes. A
         WebView only grants getUserMedia when the app also holds the OS
         permission, so declaring INTERNET alone is not enough. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- The Rosary's glow responds to the voice of whoever is praying, which
         it measures with a loudness meter over the microphone
         (public/rosary/index.html, startMeter). Leaving these undeclared did
         not merely disable that: Capacitor maps an AUDIO_CAPTURE request from
         the WebView onto RECORD_AUDIO and launches it, the framework refuses
         an undeclared permission outright, and the result came back to
         BridgeWebChromeClient twice - the second call to deny() threw
         IllegalStateException on the main thread and took the whole app down.
         That was the Rosary "crashing": a FATAL EXCEPTION, not a slow page.
         MODIFY_AUDIO_SETTINGS is requested alongside RECORD_AUDIO by that
         same Capacitor code path, so both have to be here. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Parish detection and walking directions. Coarse is listed too so the
         app still installs and degrades gracefully on a device that refuses
         precise location. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Optional, deliberately. Marking the camera required would stop the
         app installing on any device without one, and every camera feature
         already has a working fallback (manual station picking). -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
